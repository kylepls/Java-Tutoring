Repeats a statement until it becomes true, just like a while loop!

Syntax:
    
    for (<variable declaration>; <condition>; <change>) {
        // repeat stuff
    }

Example:
    
    for (int i = 0; i < 10; i++) {
        System.out.println(i);
    }
    // prints 0-9